{
  "collectionName": "components_profile_hiring_needs",
  "info": { "displayName": "Hiring need", "icon": "user-plus", "description": "What an employer registrant is actually looking for — the employer equivalent of a jobseeker's experience section." },
  "options": {},
  "attributes": {
    "roleTitle": { "type": "string", "required": true, "maxLength": 160 },
    "occupationCode": { "type": "string", "maxLength": 16 },
    "sector": { "type": "string", "maxLength": 120 },
    "vacancies": { "type": "integer", "min": 0, "max": 100000 },
    "destinationCountry": { "type": "string", "maxLength": 80 },
    "startFrom": { "type": "date" },
    "notes": { "type": "text" }
  }
}
